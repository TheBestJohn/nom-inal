# Rust
/backend/target/
**/*.rs.bk

# Node
node_modules/
/frontend/dist/
/frontend/.vite/
# TypeScript incremental build state: a machine-local artifact that changes on
# every build and was committed by mistake.
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.idea/
.vscode/

# Agent worktrees are scratch checkouts, not project files.
.claude/worktrees/
